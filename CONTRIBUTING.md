# 参与开发

先阅读 README 的平台与质量边界。修复请提供复现步骤和脱敏的最小输入；新增能力需说明支持与不支持的范围。PR 应包含相关回归验证，避免用固定示例替代真实识别。

Linux 发行使用 requirements-linux.lock。修改模型、ONNX Runtime 或 Qt 后，必须重新执行 pytest、安装包 --self-test、固定样本质量报告及实际 X11 验证。所有新增第三方模型和资源必须记录来源、版本、许可证及 SHA-256。截图和识别结果不能默认上传、落盘或写入剪贴板。
