# 变更记录

## 1.4.8 · QQ 风格手动长截图

- 修复 macOS 录屏定时唤醒延迟；使用原生严格计时器，区域三次重复及窗口录制均通过原有验收门槛。

- 选区外蒙板压暗，选区内保持实时页面，左上尺寸标签随长图增长。
- 简洁工具条提供编辑、保存、取消和完成复制；绿色对勾直接复制，取消不产生结果。
- 长图预览优先位于工具条下方并右对齐，空间不足时放在左侧，全屏时放到左上角。
- 蒙板和边框不拦截鼠标、滚轮；捕获排除标签、工具条和预览，保留手动滚动。

## 1.4.7 · 长截图范围边框

- 手动长截图期间保留蓝色选区边框，清楚显示当前截取范围。
- 边框不接收鼠标或滚轮，不影响页面操作；结束后自动关闭。
- 边框优先位于选区外，全屏及贴边时位于屏幕内侧，采集时排除，避免混入长图。

## 1.4.6 · 手动滚动长截图

- 长截图改为用户自行滚动，程序只采集和拼接，不移动鼠标或发送滚轮。
- 暂停滚动、到达底部均不会自动结束；点击小预览上的完成按钮采集最后一屏。
- 滚动过快、向上回滚或重叠不足时保留已捕获内容，提示调整位置后继续。
- 保留选区左侧／全屏左上角小预览，静止时不反复隐藏。

## 1.4.5 · 截图工具条与长图小预览

- 截图使用白色紧凑图标工具条，标注时展开颜色、粗细／字号选项，补充椭圆与识别翻译入口，保留已有截图功能。
- 长截图使用 176 像素宽的小预览，放在选区左侧；全屏或左侧空间不足时放在所在屏幕左上角。
- 首帧即显示缩略图，每帧更新；选区外的预览保持显示，选区内的预览在输入及捕获时自动隐藏。

## 1.4.4 · 长截图修复

- 修复高 DPI 下混用逻辑坐标与原生鼠标坐标，导致滚轮没有作用到选区、只截取首屏。
- 发送滚轮前隐藏预览窗，避免预览拦截滚动；等待窗口收起后再抓图。
- 重叠搜索覆盖完整视口，修复大于 600 像素重叠时提前结束；按选区高度限制滚动步长。
- 无滚动时提示重新选择正文；模糊重复内容不再猜测拼接，保留已捕获部分。

## 1.4.1 · 2026-09-20

- 修复截图窗口被系统工作区压缩、底部收藏栏未遮挡及背景缩放失真。
- 录屏面板新增文件页，按时间展示保存目录中的视频、大小与修改时间；三点菜单提供播放、打开所在文件夹和删除。删除先确认，使用系统回收站。
- 列表随录制完成、保存位置和目录内容变化刷新；隐藏录制中的临时文件。

## 1.3.0 · 2026-09-20 · Linux X11 正式版

### 图片工作台

新增原图／结果并排校对、PNG/JPEG 打开和拖入、箭头／矩形／文字／画笔／实色遮盖、撤销／重做、图片和文本导出。工作台、设置和截图工具条统一采用浅雾蓝与青绿色界面。

### 可靠性修复

- 生产环境不再回退到固定示例 OCR；未实现的云端选项隐藏。
- 所有 UI 识别入口统一异步任务，真实推理使用独立进程；旧任务、取消任务和图像修改后的结果不再交付。
- 识别、翻译、更新检查不自动改剪贴板；截图不自动保存。
- 标注变化使旧文字失效，输出使用合并后的当前图像。
- 区域输出从已确认的冻结快照裁剪，修复单屏高 DPI 降采样。
- 检查 QImage 编码和原子文件提交结果，失败不报告成功。
- 修复 RGB888 行填充导致部分宽度长截图失败。
- 长截图不可靠匹配／超限时停止，保留此前内容；关闭预览终止会话，恢复鼠标位置，采样时隐藏预览。
- 修复设置关闭无法重开、冲突配置泄漏、热键注册失败丢失旧监听、损坏配置启动失败。
- 修复同语种翻译强行反转方向、词典降级提示被遮蔽、更新失败被误报为最新。
- 增加输入尺寸限制、贴图数量限制和显示变化处理。

### 验证和范围

补充 GUI 行为回归、独立进程取消／超时恢复测试，以及真实本地 OCR 工作台冒烟。CI 的 Qt job 现在运行 pytest，不再仅导入模块。移除文档中尚未实现的表格、代码保真、付费功能和未实测性能承诺。

新增代码可见缩进恢复、简单有边框表格、单元格编辑／原图定位、Markdown／HTML+TSV／文本型 XLSX，公式风险复制拦截。补齐模型清单与校验、安装包自测、用户级桌面快捷方式、第三方声明和固定样本报告。

正式范围为 Linux x86_64 X11；Windows/macOS/Wayland、长截图、可选翻译仍为实验支持。识别质量未达到 PRD 全部目标，详细数值和未验证范围见 `validation/RELEASE-1.3.0.md`。
